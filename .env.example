# Pre-match application environment
# Copy to .env and fill in real values.
# Used by docker compose; development reads server/.env instead.

# Server
PORT=3000
NODE_ENV=production
FOOTBALL_DATA_API_KEY=
DB_PATH=./data/prematch.sqlite
