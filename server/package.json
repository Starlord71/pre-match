{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend API for the pre-match analysis tool",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "node --check src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "morgan": "^1.10.0",
    "socket.io": "^4.8.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3",
    "supertest": "^7.0.0",
    "vitest": "^3.0.5"
  }
}
