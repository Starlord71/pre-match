/**
 * Competition codes supported by the API.
 * Display names come from i18n (`leagues.<code>`).
 * @module constants/leagues
 */
export const SUPPORTED_LEAGUES = ['PL', 'PD', 'BL1', 'SA', 'FL1']

export default SUPPORTED_LEAGUES
